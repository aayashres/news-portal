# JWT Secret for authentication
# Change this to a secure random string in production
JWT_SECRET=super-secret-jwt-key-2024-news-portal-secure-random-string-abcdef123456

# Database Configuration (optional - defaults are in src/lib/db.ts)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=asdf123
DB_NAME=news_portal

# Database URL for Prisma CLI (migrations, db pull, etc.)
DATABASE_URL="mysql://root:asdf123@localhost:3306/news_portal"
